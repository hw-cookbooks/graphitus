#The default dashboards.js file is configured and written by the graphitus_instance LWRP in the graphitus cookbook, however you may specify your own template such as this one.

var graphite_url = "<%= @graphite_url %>";

var dashboards = <%= @dashboards %>;